<div class="container">
    <div class="tabs is-centered">
        <ul>
        {{#each rentDays}}
            {{ createTab }}
        {{/each}}
        </ul>
    </div>
    <div>
        {{#each rentDays}}
            {{ createRentGrid }}
        {{/each}}
    </div>
</div>
<script type="text/javascript" src="/rents.js"></script>