<div class="section">
    <div class="tabs is-centered">
        <ul>
        {{#each rentDays}}
            {{ createTab }}
        {{/each}}
        </ul>
    </div>
    <div class="px-6" id="tab_content">
        {{#each rentDays}}
            {{ createRentGrid }}
        {{/each}}
    </div>
</div>
<script type="text/javascript" src="/rents.js"></script>